[conda]
format = '\[[$symbol$environment]($style)\]'

[docker_context]
format = '\[[$symbol$context]($style)\]'

[git_branch]
format = '\[[$symbol$branch]($style)\]'

[git_status]
format = '([\[$all_status$ahead_behind\]]($style))'

[golang]
format = '\[[$symbol($version)]($style)\]'

[kubernetes]
format = '\[[$symbol$context( \($namespace\))]($style)\]'

[lua]
format = '\[[$symbol($version)]($style)\]'

[memory_usage]
format = '\[$symbol[$ram( | $swap)]($style)\]'

[os]
format = '\[[$symbol]($style)\]'

[package]
format = '\[[$symbol$version]($style)\]'

[python]
format = '\[[${symbol}${pyenv_prefix}(${version})(\($virtualenv\))]($style)\]'

[rust]
format = '\[[$symbol($version)]($style)\]'

[sudo]
format = '\[[as $symbol]($style)\]'

[time]
format = '\[[$time]($style)\]'

[username]
format = '\[[$user]($style)\]'

[zig]
format = '\[[$symbol($version)]($style)\]'

#### ROSE PINE BELOW HERE ###
#
# format = """
# $username \
# $directory \
# $git_branch \
# $git_status \
# $fill \
# $python\n  \
# $time\n  \
# [ ](fg:iris)
# """
#
# palette = "rose-pine-moon"
#
# [palettes.rose-pine-moon]
# # overlay = '#393552'
# og = '#393552'
# overlay = '#ea9a97'
# love = '#eb6f92'
# gold = '#f6c177'
# rose = '#ea9a97'
# pine = '#3e8fb0'
# foam = '#9ccfd8'
# iris = '#c4a7e7'
#
# [directory]
# format = "[](fg:overlay)[ $path ]($style)[](fg:overlay)"
# style = "bg:overlay fg:og"
# truncation_length = 3
# truncation_symbol = "…/"
#
# [directory.substitutions]
# Documents = "󰈙"
# Downloads = " "
# Music = " "
# Pictures = " "
#
# [fill]
# style = "fg:overlay"
# symbol = " "
#
# [git_branch]
# format = "[](fg:overlay)[ $symbol $branch ]($style)[](fg:overlay)"
# style = "bg:overlay fg:og"
# symbol = ""
#
# [git_status]
# style = "bg:overlay fg:og"
# format = '[](fg:overlay)[$all_status$ahead_behind]($style)[](fg:overlay)'
# disabled = false
#
# [time]
# disabled = true
# format = "[](fg:overlay)[ $time 󰴈 ]($style)[](fg:overlay)"
# style = "bg:overlay fg:rose"
# time_format = "%I:%M%P"
# use_12hr = true
#
# [username]
# disabled = true
# format = "[](fg:overlay)[ 󰧱 $user ]($style)[](fg:overlay)"
# show_always = true
# style_root = "bg:overlay fg:og"
# style_user = "bg:overlay fg:og"
#
# # Languages
#
# [c]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = " "
#
# [elixir]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = " "
#
# [elm]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = " "
#
# [golang]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = " "
#
# [haskell]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = " "
#
# [java]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = " "
#
# [julia]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = " "
#
# [nodejs]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = "󰎙 "
#
# [nim]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = "󰆥 "
#
# [rust]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = ""
#
# [scala]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay) "
# disabled = false
# symbol = " "
#
# [python]
# style = "bg:overlay fg:pine"
# format = "[](fg:overlay)[$symbol$version]($style)[](fg:overlay)[](fg:gold)[$virtualenv]($style)[](fg:gold)"
# disabled = false
# symbol = ' '
#
#
